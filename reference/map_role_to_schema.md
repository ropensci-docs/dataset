# Map R person roles to schema.org-style roles

Map R person roles to schema.org-style roles

## Usage

``` r
map_role_to_schema(role)
```

## Arguments

- role:

  A character vector of roles (e.g. `"cre"`, `"ctb"`).

## Value

A character vector with schema.org-style roles.
